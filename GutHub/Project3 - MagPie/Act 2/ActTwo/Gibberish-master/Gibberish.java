

public class Gibberish
{
    // instance variables - replace the example below with your own
    
    private String sourceString; 
    
    
    
    public Gibberish()
    {
        // initialise instance variables
        Template template = new Template();
        sourceString = template.getSourceString();
        
        
    }

    
    public String makeGibberish(WordList5000 wordlist5000)
    {
        // put your code here
        int currentSourceIndex = 0;
        String resultString = "";
        while(currentSourceIndex<sourceString.length()){
            if(currentSourceIndex<sourceString.length()){
            int x = sourceString.indexOf()
            }
            
            else if(currentSourceIndex>sourceString.length()){
                
            
            }
            
        }
        
   }
}
